// API base URL for the static chat/admin pages (served from GitHub Pages).
// Local dev: keep the loopback default. For a hosted backend, set APP_API_BASE
// to the deployed URL, e.g. "https://ai-agent-backend.onrender.com".
window.APP_API_BASE = "http://127.0.0.1:8000";
